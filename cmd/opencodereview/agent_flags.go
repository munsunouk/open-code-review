package main

const (
	agentRepoFlagHelp = "repository root for git targets; with --scan, directory for path resolution and freshness (defaults from bundle target)"
	agentBundleIndexFlagHelp = "manifest bundle index (required when the bundle file contains multiple bundles)"
)
